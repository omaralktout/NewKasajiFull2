
  # Stripe-like website design

  This is a code bundle for Stripe-like website design. The original project is available at https://www.figma.com/design/fhGy2jQ87sSwtL3OWw1hpX/Stripe-like-website-design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  